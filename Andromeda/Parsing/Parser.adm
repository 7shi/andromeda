class Parser
{
    static function Create(tokenizer : Tokenizer, parent : BlockBase)
    {
        return new Parser;
    }

    function Parse { return new NodeBase[0]; }
    function ParseExpressions { return new NodeBase[0]; }
}
