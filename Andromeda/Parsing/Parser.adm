class Parser
{
    static function Create(tokenizer : Tokenizer, parent : BlockBase)
    {
        return new Parser;
    }

    function ParseExpressions
    {
        return new NodeBase[0];
    }
}
