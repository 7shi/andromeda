const string VERSION = "Andromeda ver.1.0.2011.0101";

printfln(VERSION);
var start = DateTime.Now;

I386.Test0();
MMX.Test();
SSE2.Test();

var end = DateTime.Now;
printfln("%dms", end.Sub(start).TotalMilliseconds);

{ _Finish _finish; }
